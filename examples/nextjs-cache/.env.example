# DBX connection for this tenant.
# Provision a tenant and mint a writer key first (dashboard or examples/quickstart.py).
DBX_URL=redis://127.0.0.1:6380
DBX_TENANT=acme-corp
DBX_KEY_ID=key-id
DBX_SECRET=one-time-key-secret
